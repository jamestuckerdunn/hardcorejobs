# ==============================================================================
# HARDCOREJOBS - Environment Variables
# ==============================================================================
# Copy this file to .env.local for local development
# Never commit actual secrets - use Vercel/hosting platform for production

# ==============================================================================
# DATABASE (Required)
# ==============================================================================
# Neon/Vercel Postgres connection strings
POSTGRES_URL=postgresql://user:password@host/database?sslmode=require
DATABASE_URL=${POSTGRES_URL}

# ==============================================================================
# AUTHENTICATION - Clerk (Required)
# ==============================================================================
# Get these from: https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Clerk redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding

# ==============================================================================
# PAYMENTS - Stripe (Required for payment features)
# ==============================================================================
# Get these from: https://dashboard.stripe.com/
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe price IDs (create in Stripe Dashboard > Products)
STRIPE_FEATURED_JOB_PRICE_ID=price_...
STRIPE_RESUME_DB_PRICE_ID=price_...

# ==============================================================================
# APPLICATION SETTINGS (Required)
# ==============================================================================
# Base URL of your application (no trailing slash)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Secret for cron job authentication (min 16 characters)
# Generate with: openssl rand -base64 32
CRON_SECRET=your-secure-secret-here-min-16-chars

# Environment
NODE_ENV=development

# ==============================================================================
# JOB AGGREGATOR APIs (Optional)
# ==============================================================================
# Jobs will be fetched from APIs that have keys configured
# The aggregator works with any subset of these APIs

# Adzuna - https://developer.adzuna.com/
# Free tier: 250 calls/month
ADZUNA_APP_ID=
ADZUNA_API_KEY=

# JSearch via RapidAPI - https://rapidapi.com/letscrape-6bRBa3QguO5/api/jsearch
# Free tier: 200 calls/month
RAPIDAPI_KEY=

# The Muse - https://www.themuse.com/developers/api/v2
# Works without key, but higher rate limits with key
THEMUSE_API_KEY=

# ==============================================================================
# EMAIL (Optional - for notifications)
# ==============================================================================
# Resend - https://resend.com/
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ==============================================================================
# PRODUCTION CONFIGURATION
# ==============================================================================
# These are optional and have sensible defaults

# Featured job listing duration in days (default: 30)
FEATURED_JOB_DURATION_DAYS=30

# Resume database access duration in months (default: 1)
RESUME_DB_DURATION_MONTHS=1

# Jobs API cache TTL in seconds (default: 300 = 5 minutes)
JOBS_CACHE_TTL_SECONDS=300

# Log level: debug, info, warn, error (default: info in dev, warn in prod)
LOG_LEVEL=info
