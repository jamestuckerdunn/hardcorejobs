// Re-export all job card components for backwards compatibility
export { JobCard } from "./job-card-base";
export { JobCardCompact } from "./job-card-compact";
export { FeaturedJobCard } from "./featured-job-card";

// Re-export Job type for backwards compatibility
export type { Job } from "@/types";
