name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: hardcorejobs/package-lock.json

      - name: Install dependencies
        working-directory: hardcorejobs
        run: npm ci

      - name: Run ESLint
        working-directory: hardcorejobs
        run: npm run lint

      - name: Run TypeScript type check
        working-directory: hardcorejobs
        run: npm run type-check

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: hardcorejobs/package-lock.json

      - name: Install dependencies
        working-directory: hardcorejobs
        run: npm ci

      - name: Run unit tests with coverage
        working-directory: hardcorejobs
        run: npm run test:coverage

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          directory: hardcorejobs/coverage
          fail_ci_if_error: false

  e2e-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: hardcorejobs/package-lock.json

      - name: Install dependencies
        working-directory: hardcorejobs
        run: npm ci

      - name: Install Playwright browsers
        working-directory: hardcorejobs
        run: npx playwright install --with-deps

      - name: Run E2E tests
        working-directory: hardcorejobs
        run: npm run test:e2e
        env:
          POSTGRES_URL: postgresql://test:test@localhost/test

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: hardcorejobs/playwright-report/
          retention-days: 7

  build:
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, unit-tests]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: hardcorejobs/package-lock.json

      - name: Install dependencies
        working-directory: hardcorejobs
        run: npm ci

      - name: Build application
        working-directory: hardcorejobs
        run: npm run build
        env:
          POSTGRES_URL: postgresql://test:test@localhost/test
